var cart = [];

function getCart() {
 return cart;
}

function setCart(c) {
  cart = c;
  return cart;
}

function addToCart(itemName) {
  var newObject = {`itemName:${itemName}, ItemPrice:${itemPrice} `}

}
